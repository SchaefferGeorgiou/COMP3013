using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ChipStack : MonoBehaviour
{
    //declaring variables
    [SerializeField]
    float chipHeight; //a number for the height at which the next chip shall be stacked  
    [SerializeField]
    GameObject[] stackBases; //an array of gameObjects that are to be empty and used to position each chip stack

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
