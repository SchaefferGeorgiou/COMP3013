using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Chip : MonoBehaviour
{
    const int hundredChip = 100, 
              fiftyChip = 50, 
              twentyChip = 20, 
              tenChip = 10, 
              fiveChip = 5, 
              unitChip = 1;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
